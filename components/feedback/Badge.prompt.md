Compact count/label badge for nav items, tabs and counters.

```jsx
<Badge tone="brand">3</Badge>
<Badge tone="soft">New</Badge>
```

Tones: `brand` (deep teal), `ink` (dark), `soft` (teal tint), `gold` (premium), `outline`.
