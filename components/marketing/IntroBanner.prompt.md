The amber→purple→indigo intro banner that opens each pamphlet/page.

```jsx
<IntroBanner icon={<i data-lucide="building-2" />}>
  As your technology partner, we handle it all under one roof — from the front entrance to the back office.
</IntroBanner>
```

Feed it an offering's `intro` string from `services.ts`.
